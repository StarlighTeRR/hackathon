version: '3'
services:
  frontend:
    build:
      context: ./Frontend
    ports:
      - "3030:3030"
    volumes:
      - ./Frontend:/app
      - /app/node_modules
    command: npm run dev
    # depends_on:
    #   - backend

  # backend:
  #   build:
  #     context: ./Backend
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./Backend:/app
  #   command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"

volumes:
  postgres_data:
